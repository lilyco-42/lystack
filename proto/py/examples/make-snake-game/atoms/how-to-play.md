# 贪吃蛇规则与操作

- 网格 24×16，穿墙环绕（右边缘出去 → 左边缘进来）。
- `@` 蛇头，`O` 蛇身，`*` 食物；吃到食物 +1 分并变长。
- 禁止 180° 掉头；撞到自己的身体 → 死亡。
- 自检：`python snake.py --selftest 200 --seed 7`，200 步脚本化走位（先上后右再一路向右直线环绕），确定性存活，退出码 0。

> 关联：`ref` 字段预留视频寻址——未来从"如何学 Rust/Pygame"教程视频切出的帧图 + OCR 文字会挂到 `atoms[].ref = video:<id>#t=start,end`。
