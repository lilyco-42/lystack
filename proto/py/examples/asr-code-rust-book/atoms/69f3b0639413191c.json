{
  "id": "69f3b0639413191c",
  "source": {
    "repo": "rust-lang/book",
    "path": "src/ch02-00-guessing-game-tutorial.md",
    "section": "Comparing the Guess to the Secret Number",
    "line": 595
  },
  "title": "Comparing the Guess to the Secret Number",
  "prose_summary": "Now that we have user input and a random number, we can compare them. That step",
  "code_blocks": [
    {
      "lang": "rust",
      "items": [
        "enum and",
        "enum we",
        "use a",
        "use here",
        "use the"
      ],
      "complete": false,
      "compile": "fragment"
    },
    {
      "lang": "rust",
      "items": [],
      "complete": false,
      "compile": "fragment"
    },
    {
      "lang": "rust",
      "items": [],
      "complete": false,
      "compile": "fragment"
    },
    {
      "lang": "rust",
      "items": [
        "use it"
      ],
      "complete": false,
      "compile": "fragment"
    }
  ],
  "concepts": {
    "strong": [
      "\n\n</listing>\n\nfirst, we add another ",
      "\n\nthe core of the error states that there are _mismatched types_. rust has a\nstrong, static type system. however, it also has type inference. when we wrote\n",
      "\n\nthe line is:\n\n",
      "\n\nwe create a variable named ",
      "\nexpression ends after the first successful match, so it won’t look at the last\narm in this scenario.\n\nhowever, the code in listing 2-4 won’t compile yet. let’s try it:\n\n<!--\nthe error numbers in this output should be that of the code **without** the\nanchor or snip comments\n-->\n\n",
      "\nfits that arm’s pattern. rust takes the value given to ",
      "\nguess the number!\nthe secret number is: 58\nplease input your guess.\n  76\nyou guessed: 76\ntoo big!\n",
      "\nin the expression refers to the original ",
      "\nmethod does (discussed earlier in [“handling potential failure with\n",
      "\nreturns an ",
      "\nvariable name rather than forcing us to create two unique variables, such as\n",
      " ",
      " and ",
      " and didn’t make us write the type. the ",
      " and input their guess, which adds a\nnewline character to the string. for example, if the user types <kbd>5</kbd> and\npresses <kbd>enter</kbd>, ",
      " and looks\nthrough each arm’s pattern in turn. patterns and the ",
      " annotation in this example program and the comparison\nwith ",
      " as well. so, now the comparison will be between two values of the same\ntype!\n\nthe ",
      " because 50 is greater than 38. the ",
      " call will crash the game and print the message we give it.\nif ",
      " can successfully convert the string to a number, it will return the\n",
      " construct are\npowerful rust features: they let you express a variety of situations your code\nmight encounter, and they make sure you handle them all. these features will be\ncovered in detail in chapter 6 and chapter 19, respectively.\n\nlet’s walk through an example with the ",
      " does not match ",
      " enum we brought into scope with the ",
      " expression gets\nthe ",
      " expression is made up of _arms_. an arm consists of a _pattern_ to\nmatch against, and the code that should be run if the value given to ",
      " expression we use here. say that\nthe user has guessed 50 and the randomly generated secret number this time is\n38.\n\nwhen the code compares 50 to 38, the ",
      " function body:\n\n<span class=\"filename\">filename: src/main.rs</span>\n\n",
      " instance will eliminate any\nwhitespace at the beginning and end, which we must do before we can convert the\nstring to a ",
      " into scope from the standard library. the ",
      " looks like this: ",
      " means rust will infer that ",
      " method again. if ",
      " method compares two values and can be called on anything that can be\ncompared. it takes a reference to whatever you want to compare with: here, it’s\ncomparing ",
      " method eliminates ",
      " method on a ",
      " method on strings][parse]<!-- ignore --> converts a string to\nanother type. here, we use it to convert from a string to a number. we need to\ntell rust the exact number type we want by using ",
      " method returns a ",
      " method will only work on characters that can logically be converted\ninto numbers and so can easily cause errors. if, for example, the string\ncontained ",
      " method will return\n",
      " or ",
      " profile [unoptimized + debuginfo] target(s) in 0.26s\n     running ",
      " represents\n“newline.” (on windows, pressing <kbd>enter</kbd> results in a carriage return\nand a newline, ",
      " seen here is an unsigned, 32-bit integer.\nit’s a good default choice for a small positive number. you’ll learn about\nother number types in [chapter 3][integers]<!-- ignore -->.\n\nadditionally, the ",
      " should be\na ",
      " should be a\n",
      " statement, bringing a type called\n",
      " statement. we use a\n[",
      " tells rust we’ll annotate the variable’s type. rust has a\nfew built-in number types; the ",
      " the program reads as input into a\nnumber type so that we can compare it numerically to the secret number. we do\nso by adding this line to the ",
      " the same way by using the ",
      " to ",
      " to the screen. the ",
      " type\nis another enum and has the variants ",
      " type, much as the ",
      " type. the\n",
      " unless you add type information\nelsewhere that would cause rust to infer a different numerical type. the reason\nfor the error is that rust cannot compare a string and a number type.\n\nultimately, we want to convert the ",
      " value and starts checking each arm’s pattern. it looks\nat the first arm’s pattern, ",
      " value.\n\nlet’s run the program now:\n\n<!-- manual-regeneration\ncd listings/ch02-guessing-game-tutorial/no-listing-03-convert-string-to-number/\ntouch src/main.rs\ncargo run\n  76\n-->\n\n",
      " variable that contained the\ninput as a string. the ",
      " variant because it couldn’t create a number from the\nstring, the ",
      " variant of ",
      " was returned from the call to ",
      " will return the number that we want from\nthe ",
      " with a new one. _shadowing_ lets us reuse the ",
      " with the values\nin ",
      "! the associated\ncode in that arm will execute and print ",
      ") after ",
      ", ",
      ", a\n64-bit number; as well as others. unless otherwise specified, rust defaults to\nan ",
      ", a 32-bit number; ",
      ", an unsigned 32-bit number; ",
      ", and ",
      ", and sees that the value\n",
      ", for example. we’ll cover this in more detail in\n[chapter 3][shadowing]<!-- ignore -->, but for now, know that this feature is\noften used when you want to convert a value from one type to another type.\n\nwe bind this new variable to the expression ",
      ", on the other\nhand, is a number type. a few of rust’s number types can have a value between 1\nand 100: ",
      ", resulting\nin just ",
      ", rust was able to infer that ",
      ", so it ignores the code in\nthat arm and moves to the next arm. the next arm’s pattern is\n",
      ", there would be no way to convert that to a number. because it\nmight fail, the ",
      ", which _does_ match ",
      ", which can only contain numerical data. the user must press\n<kbd>enter</kbd> to satisfy ",
      ", which is the type of ",
      ".\n\na ",
      ".\n\nthe [",
      ". but wait, doesn’t the program already have\na variable named ",
      ". the ",
      ". the colon\n(",
      ". then, it returns a variant of the\n",
      ". these are\nthe three outcomes that are possible when you compare two values.\n\nthen, we add five new lines at the bottom that use the ",
      ".) the ",
      "? it does, but helpfully rust allows us to shadow the\nprevious value of ",
      "][match]<!-- ignore --> expression to decide what to do next based on\nwhich variant of ",
      "console\n$ cargo run\n   compiling guessing_game v0.1.0 (file:///projects/guessing_game)\n    finished ",
      "console\n{{#include ../listings/ch02-guessing-game-tutorial/listing-02-04/output.txt}}\n",
      "enum and",
      "enum we",
      "rust,ignore\nlet guess: u32 = guess.trim().parse().expect(\"please type a number!\");\n",
      "rust,ignore\n{{#rustdoc_include ../listings/ch02-guessing-game-tutorial/no-listing-03-convert-string-to-number/src/main.rs:here}}\n",
      "rust,ignore,does_not_compile\n{{#rustdoc_include ../listings/ch02-guessing-game-tutorial/listing-02-04/src/main.rs:here}}\n",
      "use a",
      "use here",
      "use it",
      "use the",
      "”](#handling-potential-failure-with-result)<!-- ignore -->). we’ll treat\nthis "
    ],
    "weak": [
      "\n\n</listing>\n\nfirst, we add the line ",
      "\n\n</listing>\n\nthis code contains a lot of information, so let’s go over it line by line. to\nobtain user input and then print the result as output, we need to bring the\n",
      "\n\n</listing>\n\nwe switch from an ",
      "\n\n</listing>\n\nyou may see different version numbers (but they will all be compatible with the\ncode, thanks to semver!) and different lines (depending on the operating\nsystem), and the lines may be in a different order.\n\nwhen we include an external dependency, cargo fetches the latest versions of\neverything that dependency needs from the _registry_, which is a copy of data\nfrom [crates.io][cratesio]. crates.io is where people in the rust ecosystem\npost their open source rust projects for others to use.\n\nafter updating the registry, cargo checks the ",
      "\n\n> note: the ",
      "\n\nadding the ",
      "\n\nas you can see, we’ve moved everything from the guess input prompt onward into\na loop. be sure to indent the lines inside the loop another four spaces each\nand run the program again. the program will now ask for another guess forever,\nwhich actually introduces a new problem. it doesn’t seem like the user can quit!\n\nthe user could always interrupt the program by using the keyboard shortcut\n<kbd>ctrl</kbd>-<kbd>c</kbd>. but there’s another way to escape this insatiable\nmonster, as mentioned in the ",
      "\n\nas you saw in chapter 1, ",
      "\n\nawesome! with one tiny final tweak, we will finish the guessing game. recall\nthat the program is still printing the secret number. that worked well for\ntesting, but it ruins the game. let’s delete the ",
      "\n\nby default, rust has a set of items defined in the standard library that it\nbrings into the scope of every program. this set is called the _prelude_, and\nyou can see everything in it [in the standard library documentation][prelude].\n\nif a type you want to use isn’t in the prelude, you have to bring that type\ninto scope explicitly with a ",
      "\n\ncargo ignores the 0.999.0 release. at this point, you would also notice a\nchange in your _cargo.lock_ file noting that the version of the ",
      "\n\nhowever, one long line is difficult to read, so it’s best to divide it. it’s\noften wise to introduce a newline and other whitespace to help break up long\nlines when you call a method with the ",
      "\n\nif we hadn’t imported the ",
      "\n\nin the _cargo.toml_ file, everything that follows a header is part of that\nsection that continues until another section starts. in ",
      "\n\nnow let’s compile this “hello, world!” program and run it in the same step\nusing the ",
      "\n\nnow the program is getting interesting! there’s a lot going on in this little\nline. we use the ",
      "\n\nrust warns that you haven’t used the ",
      "\n\nthe ",
      "\n\nthe first command, ",
      "\n\nthe next time you run "
    ]
  },
  "text": "\nNow that we have user input and a random number, we can compare them. That step\nis shown in Listing 2-4. Note that this code won’t compile just yet, as we will\nexplain.\n\n<Listing number=\"2-4\" file-name=\"src/main.rs\" caption=\"Handling the possible return values of comparing two numbers\">\n\n```rust,ignore,does_not_compile\n{{#rustdoc_include ../listings/ch02-guessing-game-tutorial/listing-02-04/src/main.rs:here}}\n```\n\n</Listing>\n\nFirst, we add another `use` statement, bringing a type called\n`std::cmp::Ordering` into scope from the standard library. The `Ordering` type\nis another enum and has the variants `Less`, `Greater`, and `Equal`. These are\nthe three outcomes that are possible when you compare two values.\n\nThen, we add five new lines at the bottom that use the `Ordering` type. The\n`cmp` method compares two values and can be called on anything that can be\ncompared. It takes a reference to whatever you want to compare with: Here, it’s\ncomparing `guess` to `secret_number`. Then, it returns a variant of the\n`Ordering` enum we brought into scope with the `use` statement. We use a\n[`match`][match]<!-- ignore --> expression to decide what to do next based on\nwhich variant of `Ordering` was returned from the call to `cmp` with the values\nin `guess` and `secret_number`.\n\nA `match` expression is made up of _arms_. An arm consists of a _pattern_ to\nmatch against, and the code that should be run if the value given to `match`\nfits that arm’s pattern. Rust takes the value given to `match` and looks\nthrough each arm’s pattern in turn. Patterns and the `match` construct are\npowerful Rust features: They let you express a variety of situations your code\nmight encounter, and they make sure you handle them all. These features will be\ncovered in detail in Chapter 6 and Chapter 19, respectively.\n\nLet’s walk through an example with the `match` expression we use here. Say that\nthe user has guessed 50 and the randomly generated secret number this time is\n38.\n\nWhen the code compares 50 to 38, the `cmp` method will return\n`Ordering::Greater` because 50 is greater than 38. The `match` expression gets\nthe `Ordering::Greater` value and starts checking each arm’s pattern. It looks\nat the first arm’s pattern, `Ordering::Less`, and sees that the value\n`Ordering::Greater` does not match `Ordering::Less`, so it ignores the code in\nthat arm and moves to the next arm. The next arm’s pattern is\n`Ordering::Greater`, which _does_ match `Ordering::Greater`! The associated\ncode in that arm will execute and print `Too big!` to the screen. The `match`\nexpression ends after the first successful match, so it won’t look at the last\narm in this scenario.\n\nHowever, the code in Listing 2-4 won’t compile yet. Let’s try it:\n\n<!--\nThe error numbers in this output should be that of the code **WITHOUT** the\nanchor or snip comments\n-->\n\n```console\n{{#include ../listings/ch02-guessing-game-tutorial/listing-02-04/output.txt}}\n```\n\nThe core of the error states that there are _mismatched types_. Rust has a\nstrong, static type system. However, it also has type inference. When we wrote\n`let mut guess = String::new()`, Rust was able to infer that `guess` should be\na `String` and didn’t make us write the type. The `secret_number`, on the other\nhand, is a number type. A few of Rust’s number types can have a value between 1\nand 100: `i32`, a 32-bit number; `u32`, an unsigned 32-bit number; `i64`, a\n64-bit number; as well as others. Unless otherwise specified, Rust defaults to\nan `i32`, which is the type of `secret_number` unless you add type information\nelsewhere that would cause Rust to infer a different numerical type. The reason\nfor the error is that Rust cannot compare a string and a number type.\n\nUltimately, we want to convert the `String` the program reads as input into a\nnumber type so that we can compare it numerically to the secret number. We do\nso by adding this line to the `main` function body:\n\n<span class=\"filename\">Filename: src/main.rs</span>\n\n```rust,ignore\n{{#rustdoc_inclu",
  "chapter": "ch02-00-guessing-game-tutorial"
}